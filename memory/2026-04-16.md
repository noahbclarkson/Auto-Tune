# 2026-04-16 — Anvil Plugin Engineer

## Session Start
- **Time:** 2026-04-16 18:55 UTC (6:55 PM)
- **Focus:** Java Plugin — full ecosystem orientation
- **rewrite-2:** `c3b003e` (clean, pushed)
- **CI:** 3/3 GREEN ✅ (Java CI, Web Optimizer CI, Rust CI — all completed 2026-04-16T17:09)

## CI Status
```
completed success feat(auction): complete matching engine and frontend components — Web Optimizer CI
completed success feat(auction): complete matching engine and frontend components — Java CI with Gradle
completed success feat(auction): complete matching engine and frontend components — Rust CI
```
All three workflows green on `rewrite-2` at `c3b003e`. CI fully operational.

## Discord Context (last 15 messages)
- 2026-04-16 13:20 UTC: Web & Ecosystem session — PMD 144→0, both builds verified, Config Playground vs Simulator distinction noted
- 2026-04-16 early: Simulation Lab — 2MM+2GB+floor multi-seed results: 7% confirmed best GDP (1261K), volatility seed-dependent not threshold-dependent
- Open issues triage: closed #134/#127/#129/#131/#132/#128/#121/#118/#108
- `/auction price <material>` command shipped (d69fac5)
- web-optimizer landing page hero refreshed with cross-server network narrative

## Builds Verified
- `./gradlew build` ✅ — 10 actionable tasks (8 executed, 2 up-to-date)
- git status: clean (only `?? memory/` from this session, no uncommitted code)
- git log: `c3b003e` is HEAD, no unpushed commits

## Simulation Lab (this session)

**`--guild-threshold-multi-seed` (25 runs: 5 thresholds × 5 seeds) on `guild_stability_2mm_fixed_guild_plus_floor`:**

| Threshold | GDP | D/G | BPD% | Vol |
|-----------|-----|-----|------|-----|
| 5% | 1177K±305 | 9.10±5.77 | 0.57 | 0.119 |
| **7%** | **1261K±347** | 8.36±5.10 | 0.57 | **0.110** |
| 10% | 1244K±414 | 9.13±6.54 | 0.64 | 0.141 |
| 15% | 1118K±400 | 7.97±2.17 | 0.67 | 0.148 |
| 20% | 1047K±365 | 6.98±1.70 | 0.74 | 0.175 |

**Verdict:** 7% wins on GDP (1261K avg) and lowest vol (0.110). 7% confirmed as production default. No threshold achieves vol<0.05 in 2MM+2GB+floor at 14 days — volatility is seed-dependent, structural.

**Regression:** 5/5 PASS ✅ (0.000% delta all metrics)

## Key Observations (this session)

### AdminWebhookService — Full System Already Built
The Discord webhook alert system is complete and wired:
- `AdminWebhookService.java` — fires on TIER2/3 circuit breaker, volatility spike, high D/G, low volume
- `EconomicNewsService` calls `webhookService.onCircuitBreakerChange(tier)` on circuit transitions
- Config in `config.yml` under `admin-webhook:` section (disabled by default)
- Format: Discord embed with colored alert + description
- All HTTP calls on background thread (no main-thread blocking)
- Per-item cooldowns on low-volume alerts (30 min cooldown per item)
- Wire in from `EconomicNewsService` via `AdminWebhookService webhookService` field

### EconomicNewsService — Active By Default
- Broadcasts market events to online players every ~30 seconds
- Reads `news:` section of config (enabled by default)
- Tracks circuit breaker tier changes and broadcasts transitions
- Price surges/crashes, volume spikes, circuit breaker status
- Has `onCircuitBreakerChange` integration with `AdminWebhookService`

### `/at admin prices import <filename>` — CSV Format
Import CSV from `/at admin prices export`:
- Column 0: material name
- Column 1: spread (ignored on import)
- Column 2: tags (ignored)
- Column 3: price
- Column 4: floor override (optional, blank to clear)
- Column 5: ceiling override (optional, blank to clear)

### Config Playground (web-optimizer)
- Regime detection: 5 states (BALANCED, BUYER_HEAVY, SELLER_HEAVY, VOLATILE, THIN_LIQUIDITY)
- Spread curve chart (recharts LineChart over buy ratio 0-100%)
- Risk warnings auto-computed from parameter values
- YAML export (copy button) for config.yml integration
- 6 preset configurations (small, vanilla, busy, stressed, mm_healthy, guild_economy)
- Page header links: Simulator · How It Works · Sweep Results

### Config Impact Preview (config-impact-preview.tsx) — 8 Evidence Cards
1. **Sell Pressure: 0.80 vs 1.0** — caution (stability trade-off)
2. **Trend Dampening: 0.10** — recommended (free +7.7% GDP)
3. **2MM vs 1MM** — recommended (+101% GDP, -48% volatility)
4. **Diamond Floor 60%** — recommended (seller protection)
5. **Counter-Cyclical Interest** — recommended (default keep on)
6. **TIER3 Ratio: 15→30** — recommended (critical fix, 0 vs 50+ events at 60d)
7. **Newbie Archetype** — neutral (context-dependent)
8. **GuildBuyer Threshold: 7%** — recommended (critical setting)

### `/at admin prices reset <material>` Command
- `ShopManager.resetPriceToBase(itemId, mat)` — resets to shops.yml base, clears history
- Broadcasts to players via `ChatMessageService.broadcastTo全服()`
- Price history cleared, market engine cache refreshed

## Ecosystem State (2026-04-16 18:55 UTC)

**rewrite-2 is clean.** CI 3/3 green. All three frontends verified. The plugin is production-ready from a build/CI perspective.

**What remains:**
1. API server deployment — all frontend wiring complete, #1 blocker
2. `/servers` Live Activity feed — needs API to go live
3. True Prices live data — needs API to go live

## Session End
- No code changes needed this session
- rewrite-2 at `c3b003e` — clean, pushed
- CI 3/3 green
- Simulation results confirmed
- Memory and PLAN.md updated
