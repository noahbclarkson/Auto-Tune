sections:
  ores:
    display: "<b><gold>Natural Resources</gold></b>"
    image: "IRON_ORE"
    pos: "1,1"
  food:
    display: "<b><gold>Food</gold></b>"
    image: "APPLE"

shops:
  apple:
    display: "<orange>Apple</orange>"
    material: "APPLE"
    price: 2.0

# The log level, can be one of:
# SEVERE, WARNING, INFO, CONFIG, FINE, FINER, FINEST, ALL
log-level: "INFO"

# The database url. This is an SQLite JDBC url, so it should be in the format of:
# jdbc:sqlite:path/to/database.db
database:
  url: "jdbc:sqlite:plugins/Auto-Tune/data.db"
